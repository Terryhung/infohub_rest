version: '2'
services:
  redis:
    image: redis:3.2-alpine
    volumes:
      - redis_data:/data
    ports:
    - "6378:6379"
  app:
    build: .
    ports:
    - "8787:8787"
    depends_on:
    - redis
    environment:
    - REDIS_HOST=redis
volumes:
  redis_data:
